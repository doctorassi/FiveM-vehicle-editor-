<?xml version="1.0" encoding="UTF-8"?>
<!-- LEGACY LOCATION - no longer used.
     The editor now writes ../handling.meta at the resource root, because
     SaveResourceFile cannot create directories. This file is read once on
     startup so an existing install keeps its tunes, then it can be deleted. -->
<CHandlingDataMgr>
  <HandlingData>
  </HandlingData>
</CHandlingDataMgr>
